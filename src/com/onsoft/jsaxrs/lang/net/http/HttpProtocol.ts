/**
 * The <code>HttpProtocol</code> enum provides values that define HTTP protocols.
 */
export enum HttpProtocol {

    /**
     * Provides the reference for the http protocol.
     */
    HTTP = 'http',
  
    /**
     * Provides the reference for the https protocol.
     */
    HTTPS = 'https'
  }